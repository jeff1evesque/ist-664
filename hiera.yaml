---
version: 5
defaults:
  datadir: /etc/puppetlabs/puppet
  data_hash: yaml_data

hierarchy:
  - name: 'general mongodb configuration'
    path: hiera/mongodb.yaml

  - name: 'mongodb cluster configuration'
    path: hiera/mongodb_cluster.yaml
